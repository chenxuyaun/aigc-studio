# 安全策略（Security Policy）

## 支持的版本

本项目为自托管/开源演进中的个人 AI 平台，当前维护 `main` 分支。

## 报告漏洞

请**不要**在公开 Issue 中提交安全漏洞（凭据、注入、越权等）。

- 首选：向项目维护者发送私密消息（GitHub Security Advisory 或维护者邮箱）
- 紧急情况：先在本地关闭受影响服务，再报告

请附上：

1. 影响模块（API / 前端 / 部署）
2. 复现步骤（最小）
3. 影响范围与可能的利用方式
4. 修复建议（可选）

## 安全基线（本项目遵循）

- `.env` 含全部密钥（JWT/DB/Redis/LLM/管理账号）——永不提交、永不打印
- 生产环境拒绝默认密钥（`APP_ENV=production` 时强校验）
- 公网仅暴露前端 5000（nginx 反代 `/api`）；API/DB/Redis 不暴露
- 平台不开放匿名注册；媒体资源走鉴权接口（无公开直出）
- 私有媒体访问 URL 签名；全局限流默认开启

## 致谢

感谢所有帮助平台变得更安全的贡献者。
