x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  frontend:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      # 只绑本机：公网访问走宿主机 nginx（yuncai.site 反代 /saios）
      - "127.0.0.1:${FRONTEND_PORT:-5000}:80"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

    logging: *default-logging
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    entrypoint: ["bash", "/docker-entrypoint.sh"]
    ports:
      # 只绑本机：公网 API 走 nginx 反代 /api（8002 不暴露公网）
      - "127.0.0.1:${API_INTERNAL_PORT:-8002}:8000"
    env_file: .env
    # 容器内以 mysql 服务名访问；本地 uvicorn（无 DATABASE_URL）仍走 SQLite
    environment:
      - DATABASE_URL=mysql+aiomysql://${MYSQL_USER:-aigc}:${MYSQL_PASSWORD:-changeme}@mysql:3306/${MYSQL_DATABASE:-aigc_studio}
      # 模型中心：容器经 host-gateway 访问宿主机 model-hub（systemd 绑 172.17.0.1）
      - MODEL_HUB_BASE_URL=http://host.docker.internal:8511
    # 让容器内 provider 的 host.docker.internal 能指向宿主机（grok2api/cpa 跑在宿主机 docker）
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - storage_data:/app/storage
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health/live')",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

    logging: *default-logging
  worker:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    command:
      [
        "celery",
        "-A",
        "app.tasks.celery_app:celery_app",
        "worker",
        "-B",
        "-l",
        "info",
        "-Q",
        "text,image,video,audio,import,maintenance",
      ]
    env_file: .env
    environment:
      - DATABASE_URL=mysql+aiomysql://${MYSQL_USER:-aigc}:${MYSQL_PASSWORD:-changeme}@mysql:3306/${MYSQL_DATABASE:-aigc_studio}
      - MODEL_HUB_BASE_URL=http://host.docker.internal:8511
      # celery 每任务 asyncio.run 新事件循环：常驻连接池会把旧 loop 的 aiomysql
      # 连接带进新 loop（"Future attached to a different loop"/Event loop is closed）。
      # worker 必须 NullPool；API 进程不受此变量影响，保持默认池。
      - DB_POOL_CLASS=null
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # worker 长跑会累积 fd（每任务事件循环/连接/临时句柄），默认 1024 上限曾耗尽
    # （OSError: Too many open files → 全部媒体任务卡 queued）。调高容器 nofile 上限。
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - storage_data:/app/storage
      - ./backups:/app/backups
    restart: unless-stopped

    logging: *default-logging
  mysql:
    image: mysql:8.4
    # 仅绑本机回环：供宿主机进程（model-hub 的 SAIOS_DB_URL=127.0.0.1:3307）读用量统计；
    # 不暴露公网。容器间仍走服务名 mysql:3306。
    ports:
      - "127.0.0.1:3307:3306"
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-aigc_studio}
      MYSQL_USER: ${MYSQL_USER:-aigc}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-changeme}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-changeme}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

    logging: *default-logging
  redis:
    image: redis:7-alpine
    # 必须开启 requirepass：worker/api 的 REDIS_URL 都带密码（REDIS_PASSWORD），
    # 若 redis 无密码则 worker 报 `AUTH called without any password configured` 死循环，
    # 整个 Celery 异步队列（图片/视频/音乐/文本/import/maintenance）全部瘫痪。
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:-changeme}", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      # 带密码 ping，否则健康检查永远假阳性（无密码也能 PONG）
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-changeme}", "--no-auth-warning", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

    logging: *default-logging
volumes:
  mysql_data:
  redis_data:
  storage_data:
